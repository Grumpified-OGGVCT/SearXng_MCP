# Example Configuration File for SearXNG MCP Server

# This file demonstrates how to configure the SearXNG MCP Server
# Copy this to .env and customize as needed

# SearXNG Instances (comma-separated)
# Default instances are provided, but you can add your own
SEARXNG_INSTANCES=https://search.sapti.me,https://searx.be,https://search.bus-hit.me

# Local SearXNG Instance (optional)
# Uncomment to use a local instance as fallback
# SEARXNG_LOCAL_INSTANCE=http://localhost:8888

# Request Timeout (seconds)
# Default: 5.0 for online instances
SEARXNG_TIMEOUT=5.0

# Local Instance Timeout (seconds)
# Default: 15.0 for local instance (usually slower)
SEARXNG_LOCAL_TIMEOUT=15.0

# Cookie Directory
# Default: ~/.searxng_mcp/cookies
# SEARXNG_COOKIE_DIR=/path/to/cookies

# Logging Level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# LOG_LEVEL=INFO

# --- MCP Configuration ---

# MCP Server Name
# MCP_SERVER_NAME=SearXNG Search

# MCP Transport
# Options: stdio, sse
# Default: stdio
# MCP_TRANSPORT=stdio

# SSE Server Configuration (if using SSE transport)
# MCP_SSE_HOST=localhost
# MCP_SSE_PORT=8000

# --- Advanced Configuration ---

# User Agent (optional)
# Customize the User-Agent header for requests
# USER_AGENT=SearXNG-MCP/0.1.0

# Max Retries
# Number of instances to try before giving up
# MAX_RETRIES=5

# Enable Debug Mode
# Set to "true" to enable verbose logging
# DEBUG=false

# --- AI Enhancement Configuration (Optional) ---

# AI Provider
# Options: openrouter, ollama, gemini
# Leave empty to disable AI enhancement
# All providers use Gemini Flash models for optimal speed/cost/quality
# SEARXNG_AI_PROVIDER=openrouter

# AI API Key
# Required if AI provider is set
# - OpenRouter: Get key from https://openrouter.ai/keys
# - Ollama Cloud: Get key from https://ollama.com/settings/keys
# - Google Gemini: Get key from https://aistudio.google.com/app/apikey
# SEARXNG_AI_API_KEY=your_api_key_here

# AI Model (Optional - auto-detected if not set)
# All providers default to Gemini Flash models:
# - OpenRouter: google/gemini-2.0-flash-exp (default)
# - Ollama Cloud: gemini-3-flash-preview:cloud (default, note :cloud tag!)
# - Gemini: Auto-detects latest Flash model (e.g., gemini-2.0-flash-exp)
# SEARXNG_AI_MODEL=

# --- Dashboard Configuration ---

# Dashboard Port
# Default: 8765
# DASHBOARD_PORT=8765

# Dashboard Host
# Default: localhost (change to 0.0.0.0 for network access)
# DASHBOARD_HOST=localhost

# Cache Configuration (future feature)
# CACHE_ENABLED=false
# CACHE_DIR=~/.searxng_mcp/cache
# CACHE_TTL=3600

# Rate Limiting (future feature)
# RATE_LIMIT_ENABLED=false
# RATE_LIMIT_REQUESTS=10
# RATE_LIMIT_PERIOD=60
